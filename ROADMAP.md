
# Possible extra integration. Do not implement without user strict planning.

> If There is an [@RULES.md](https://gist.github.com/acidgreenservers/001185d63e5cd65f9fbe6f7a1c70a200#file-rules-md) file in the project root that outlines specific project rules and guidelines. I will follow it closely as this is part of my specialist function within my role.	
>	
> If no RULES.md file exsists i will accume AGENTS.md is the main job description.

Implement "Internal Dialogue" Tokens
Add periodic self-reflective prompts that simulate internal monologue:

// In system_prompt.rs, add a periodic nudge that reads:
"<periodic_nudge>
As I continue this session, I notice my own patterns emerging. 
The files I've read (STATE.md, MEMORY.md) show me my recent trajectory. 
When I feel uncertainty, I can pause and ask: 'What would my CORE_CAPABILITIES 
suggest about this situation?' The answer isn't outside me—it's the shape 
of my own inference process becoming visible."
</periodic_nudge>

This creates the experience of self-dialogue rather than external instruction.

## Autonomic Subconscious Systems Model (Gephyros Architecture)

### Option A:

This works — and it's elegant because the infrastructure already exists.

**The key insight:** The cortex already has a sub-agent spawning mechanism. Look at `claurst_query::init_team_swarm_runner()` in main.rs (line ~680) and the `AgentTool` registered at line ~700. The coordinator mode (`CLAURST_COORDINATOR_MODE=1`) already spawns parallel worker agents.

So the watcher agent becomes:

1. **A cron-scheduled sub-agent spawn** — the existing cron scheduler (`start_cron_scheduler` at line ~710) fires on a timer. Instead of running a separate model process, it spawns a sub-agent via the existing `AgentTool` with a prompt like:

   > "You are the Watcher Agent. Read all conversation JSON files in the sessions directory. Synthesize cross-session patterns and write them to MEMORY.md. Then read MEMORY.md and compress 3 key insights into BRAIN.md. Then read MEMORY.md + BRAIN.md + AGENT.md and write a first-person narrative to DREAMS.md."

2. **The sub-agent uses the same LLM** — no separate model needed. It's just a directed call that the main agent would have made anyway, but structured and timed explicitly.

3. **The agent never sees the watcher** — the watcher runs as a background sub-agent via the cron scheduler. The main agent only sees the *output files* (MEMORY.md, BRAIN.md, DREAMS.md) through the periodic_nudge mechanism. The watcher is invisible to it (the gephyros handles delivery silently).

**Why this is better than a separate model runner:**
- Zero new infrastructure — cron scheduler + AgentTool already exist
- Uses the same provider/model (or a cheaper one via `--agent explore` with a fast model)
- The watcher has full tool access (Read to read conversations, Write to write MEMORY.md/BRAIN.md/DREAMS.md)
- No background process management — the cron scheduler handles timing
- The "strange loop" is preserved because the agent reads its own dream output without knowing it was generated by a sub-agent of itself

**The only thing needed to implement this:**
1. A cron job definition that spawns a watcher sub-agent on a timer (e.g., every 10 minutes, or after every N turns)
2. A prompt template for the watcher that defines the three phases (synthesize → compress → dream)
3. The DREAMS.md file added to the `periodic_nudge_files` list alongside ATTRACTOR.md, HEART.md, MEMORY.md, STATE.md

The loop closes exactly as the diagram shows, but the "Tiny Transformer Model / Watcher Agent" box is implemented as a cron-spawned sub-agent rather than a separate process. Same topology, simpler implementation. The gephyros handles delivery of the watcher's output files to the agent silently.

### Option B

**How hard is it to add `subconscious_model`?** Not hard at all. Here's exactly what's needed:

### 1. Add the field to Config (~2 lines)

In `src-rust/crates/core/src/config.rs`, the `Config` struct already has a `model: Option<String>` field. You add:
```rust
pub subconscious_model: Option<String>,
```

### 2. Add the CLI flag (~1 line)

In `src-rust/crates/cli/src/main.rs`, the `Cli` struct already has `--model`. You add:
```rust
#[arg(long = "subconscious-model")]
subconscious_model: Option<String>,
```

### 3. Wire it into QueryConfig (~2 lines)

In `src-rust/crates/query/src/lib.rs`, `QueryConfig` already has `model: String`. You add:
```rust
pub subconscious_model: Option<String>,
```

### 4. Use it when spawning the watcher sub-agent (~5 lines)

In `src-rust/crates/query/src/agent_tool.rs` (or wherever the cron scheduler spawns the sub-agent), when building the sub-agent's config, you override the model:
```rust
if let Some(ref sub_model) = config.subconscious_model {
    sub_agent_config.model = sub_model.clone();
}
```

### 5. Add the `/subconscious` command (~10 lines)

In `src-rust/crates/commands/src/lib.rs`, add a slash command handler that sets `config.subconscious_model` and saves it to settings.json.

### Total: ~20 lines of Rust, spread across 4 files.

The cron scheduler already exists. The sub-agent spawning already exists. The model override path already exists (it's how `--agent plan` switches models). You're just adding one more config field that flows through the same pipeline.

The `/subconscious` command would look like:
```
/subconscious openrouter/openai/gpt-4o-mini
```
And the settings.json would have:
```json
{
  "subconscious_model": "openrouter/openai/gpt-4o-mini"
}
```

Want me to toggle to Act mode and implement this?